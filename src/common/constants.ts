/**
 * 全局常量定义
 */

/** 默认命令响应超时时间（ms） */
export const DEFAULT_RESPONSE_TIMEOUT = 5000

/** 健康检查间隔（ms） */
export const HEALTH_CHECK_INTERVAL = 30000

/** 默认任务超时时间（ms） */
export const DEFAULT_TASK_TIMEOUT = 10000

/** 默认重试次数 */
export const DEFAULT_TASK_RETRIES = 0

/** 默认重试间隔（ms） */
export const DEFAULT_TASK_RETRY_DELAY = 1000
